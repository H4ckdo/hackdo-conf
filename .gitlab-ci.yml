#image: node:7.5.0

stages:
  - build
  - deploy
  

build:
  type: build
  cache:
    key: "$CI_BUILD_NAME"
    untracked: true
    
  script:
    - mkdir build/js
    - ls build

deploy:
  type: deploy
  script:
  - ls build
