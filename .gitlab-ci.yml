#image: node:7.5.0

variables:
  CI_BUILD_NAME: "build"
 
stages:
  - build
  - deploy
  

build:
  type: build
  cache:
    group: "$CI_BUILD_NAME"
    untracked: true
    paths:
        - build/js
    
  script:
    - mkdir build/js
    - ls build

deploy:
  type: deploy
  script:
  - ls build
